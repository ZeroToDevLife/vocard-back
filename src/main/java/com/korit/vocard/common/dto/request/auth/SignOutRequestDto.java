package com.korit.vocard.common.dto.request.auth;

import lombok.Getter;
import lombok.NoArgsConstructor;
import lombok.Setter;

@Getter
@Setter
@NoArgsConstructor
public class SignOutRequestDto {
    // 로그아웃 요청에 필요한 필드가 있다면 여기에 추가
    // 현재는 별도 필드 없이 빈 DTO로 사용
} 